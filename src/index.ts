/// <reference path="../types/index.d.ts" />
// DO NOT remove the above line, it makes sure
// that the typescript compiler knows about the global types

// styles, not used at the moment
// import "./styles.css";

/*
 * Components
 * All root components that permit server side rendering should be exported here
 * This is the entry point for the library
 */
export * from './components/NiceLittleComponent';
